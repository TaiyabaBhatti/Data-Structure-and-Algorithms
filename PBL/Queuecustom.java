package com.PBL;/*
package com.PBL;

import java.util.LinkedList;

public class Queuecustom {
         CropData head=new CropData();  //dummy node
     LinkedList<CropData> cropDataListQueue=new LinkedList<>();
     public void addCropData(CropData cropData) {
                cropDataListQueue.addLast(cropData);
               }


     public void deQueue() {
          cropDataListQueue.removeFirst();
     }

}*/
